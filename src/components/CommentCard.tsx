import { Image, ImageSourcePropType, View } from 'react-native';
export default function CommentCard(comment: string, img: ImageSourcePropType) {
  return (
    <View className="">
      <Image />
    </View>
  );
}
